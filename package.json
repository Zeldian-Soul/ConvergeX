{
  "name": "convergex-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:mobile": "pnpm --filter @convergex/mobile dev || (cd apps/mobile && npx expo start)",
    "start:functions": "cd apps/functions && npm run serve",
    "start:javafx": "cd apps/javafx && ./gradlew run",
    "bootstrap": "npm install && (cd apps/mobile && npm install) && (cd apps/functions && npm install) && (cd packages/shared-types && npm install)"
  },
  "devDependencies": {}
}
